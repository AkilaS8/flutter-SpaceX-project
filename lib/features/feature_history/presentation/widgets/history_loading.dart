import 'package:flutter/material.dart';

class HistoryLoadingView extends StatelessWidget {
  const HistoryLoadingView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(

    child: Center(
    child: Text("Loading"),
    ),
    );
  }
}
